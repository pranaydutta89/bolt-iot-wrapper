export enum PINS {
    zero = 0,
    one,
    two,
    three,
    four
}


export enum STATE {
    high = 'HIGH',
    low = 'LOW'
}

export enum CONSTANTS {
    defaultLoopTime = 3100,
    defaultApiDiff = 3000
}